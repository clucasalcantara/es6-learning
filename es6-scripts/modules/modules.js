// JS ES6 Study, this file contains the examples i've built when i'm studying
//modules

//This is the default declaration for modules, every module will load this
//function
export default function(text) {
    //More es6, template strings :)
    console.log(`Modules are ${text}.`);
}
